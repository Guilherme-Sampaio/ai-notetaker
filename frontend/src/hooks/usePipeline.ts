// TODO: implement usePipeline hook
// All PipelineState transitions live here — no pipeline logic in components.
// Orchestrates: useRecorder → api.processAudio → parsed summary → state.done

export {}
