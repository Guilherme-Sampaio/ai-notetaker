// TODO: implement POST /api/process route
// Pipeline: multer upload → transcribeAudio → summarizeMeeting → parseSummaryResponse → res.json
// Always call next(error) — never res.status() inline.
// multer must use memoryStorage and whitelist audio/* mime types only.

export {}
